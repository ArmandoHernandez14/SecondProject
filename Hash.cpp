#include<iostream>
#include<string>
#include"Hash.h"

int hash::Hash(string key){//It's going to manipulate the string, and turn it into a number
  int hash = 0;
  int index = key.length();
  return index;
}
